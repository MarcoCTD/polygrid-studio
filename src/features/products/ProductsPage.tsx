import { Package } from 'lucide-react';
import { PlaceholderPage } from '@/components/shared';

export function ProductsPage() {
  return (
    <PlaceholderPage
      title="Produkte"
      description="Produktkatalog mit CRUD, Tabelle, Detail-Panel und Margenrechner. Wird in Modul 02 implementiert."
      icon={Package}
    />
  );
}
