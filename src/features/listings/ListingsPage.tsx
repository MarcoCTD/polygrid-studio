import { FileText } from 'lucide-react';
import { PlaceholderPage } from '@/components/shared';

export function ListingsPage() {
  return (
    <PlaceholderPage
      title="Listings"
      description="Plattform-Listings mit Editor, Zeichenzähler und Plattformlogik. Wird in Modul 05 implementiert."
      icon={FileText}
    />
  );
}
